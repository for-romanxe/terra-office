# AI 직원 만들기 — 프로젝트 기록

> 2026-07-18 시작. "AI 직원 만드는 거 해보고 싶어"에서 출발해,
> 개념 학습 → 개인 비서 에이전트 → 도트 픽셀 사무실 → 다부서 멀티에이전트 시스템까지 완성.
> 2026-07-23부터 혼자·대회용이 아니라 **친구들과 팀으로 상시 사용** — 직급제 도입.

## 한눈에 보기

- **위치**: `~/ai-agent-lab`
- **이름**: TERRA 연구실 (구 "연성 비서실")
- **스택**: Node.js + `@anthropic-ai/sdk` (JS, .mjs) — `ant` CLI 로그인으로 API 키 없이 동작
- **실행**: `node office/server.mjs` → http://localhost:3010 (여는 순서는 바탕화면 `사무실 여는법.txt`)
- **로그**: `/tmp/office.log`

---

## 1단계 — 개념 학습 (실습 3개)

에이전트 = **시스템 프롬프트(정체성) + 도구(능력) + messages 배열(기억) + 루프**.

| 파일 | 내용 |
|---|---|
| `01-first-call.mjs` | 단순 API 호출 1회. 에이전트가 아닌 "함수" |
| `02-agent-loop.mjs` | 도구(`list_directory`) + 에이전트 루프. `stop_reason: "tool_use"` → 도구 실행 → `tool_result` 반환 → 반복 |
| `03-secretary.mjs` | 대화형 비서. 메모 저장/조회 도구 + `web_search`, REPL 방식. 메모는 `notes/`에 md로 영속 |

핵심 원리: **API는 매번 기억이 없다.** messages 배열을 우리가 계속 들고 다니는 것이 "기억"이고, 도구 실행 결과를 다시 넣어주는 반복문이 "일하는 능력"이다.

---

## 2단계 — 도트 픽셀 오피스 (`office/`)

에이전트가 일하는 모습을 **90년대 한국 사무실** 컨셉으로 시각화한 웹 UI.

- **컨셉**: 도트 픽셀 (Galmuri11 폰트, 크롬 `#191b26` + 노랑 `#ffd23e`)
- **구조**: `server.mjs`가 에이전트 이벤트를 SSE로 중계 → 브라우저가 "연기 큐"로 장면을 순서대로 재생
- 캔버스 288×180, 10×14 픽셀 스프라이트, 시간대별 하늘색 변화, 45초마다 커피 타임 연출
- 위임 시 팀원이 실장 책상 앞으로 걸어와 지시받고, 끝나면 보고하러 다시 오는 동선(L자 경로)

## 3단계 — 멀티에이전트 (실장-팀원 위임)

- 실장은 팀원별 `ask_<id>` 위임 도구를 가짐. 그 도구의 구현이 곧 **또 하나의 에이전트 루프**
- 팀원마다 별도의 messages 배열 → 각자 자기 대화 기억을 유지

## 4단계 — md 파일로 직원 정의

직원 = md 파일 하나. **채용 = md 추가 + 서버 재시작.**

```md
---
order: 1              # 연차(책상 배정 순서)
id: clerk
name: 기록 담당
model: claude-haiku-4-5   # 생략 시 claude-opus-4-8
effort: low           # 생략 시 low, 실장은 medium
color: "#33406e"
tools: save_note, list_notes, read_note, read_user_context
duty: 한 줄 소개(명패·범례에 표시)
---
본문 = 시스템 프롬프트
```

- **범례에 모델 표시** (2026-07-20): 사무실 장면 아래 범례에 이름 옆으로 `· Opus 4.8` / `· Haiku 4.5`처럼 실제 적용 모델이 붙음. 서버가 `md`의 `model:` 값을 `modelLabel()`로 축약(`claude-haiku-4-5` → `Haiku 4.5`)해 roster에 실어 보냄, 프론트는 그대로 표시만.

서버가 로드해서 실장의 위임 도구를 자동 생성하고, 프론트는 hello 이벤트의 roster로 방을 동적 구성.

---

## 5단계 — 부서제 (`office/departments/`)

### 비서실 (평상시 업무)

| 직원 | 모델 | 도구 | 역할 |
|---|---|---|---|
| 실장 | Opus 4.8 (medium) | ask_* 위임 | 판단·분배·보고 |
| 기록 담당 | **Haiku 4.5** (비용 1/5) | save/list/read_note, read_user_context, read_file | 메모 + 사장님 인사 기록·첨부 파일 열람 |
| 조사 담당 | Opus 4.8 | web_search | 웹 조사, 신속 우선 |
| 아카이버 담당 | Opus 4.8 | archive_* 5종 | 대외활동 아카이버 DB 관리 |

### 개발실 (해커톤·대회용)

| 직원 | 도구 | 역할 |
|---|---|---|
| 개발실장 | ask_* 위임 | 커밋 전 코드 점검관 검토 필수, 치명 발견 시 커밋 중단 |
| 코드 점검관 | git_status/diff/log, read_file, list_files | 시크릿·버그·품질 (치명/경고/제안 등급) |
| 통합 담당 | git_commit/branch/merge/push | 유일하게 쓰기 가능한 git 담당 |
| 스펙 점검관 | 읽기 도구 | 요구사항 대비 구현 확인 |
| 제출 점검관 | 읽기 도구 + web_fetch | 제출물·링크 점검 |
| 문서 점검관 | 읽기 도구 | README·문서 점검 |

점검관 4명은 사용자가 준 프롬프트 원문 기반. 공통 규칙: **"점검만 한다. 시키기 전엔 코드를 임의로 수정하지 않는다"** — 프롬프트가 아니라 **도구를 읽기 전용으로 줘서 시스템적으로 강제**.

`test-repo/`(고의 버그 3개: 하드코딩된 시크릿 키, off-by-one, 누락된 await)로 검증 — 점검관이 전부 적발, 실장이 원칙대로 커밋 중단.

### 물커톤실 (물커톤 = MOVE-AI 물류 해커톤 전용, 본선 8/13)

- 대회 전용 부서 — 개발실과 busy 잠금이 분리돼 **병렬로 돌아감** (해커톤 중 개발실 병목 방지)
- 구성: 물커톤 실장 + 전략 담당(대회정보 노트·PRD·리서치) + 코드 점검관 + 제출 점검관 + 통합 담당
- 기준 문서: 노트 `물커톤-대회정보` — 대회 정보(주제·규칙·마감·저장소)는 여기 하나로 관리, 미정 항목은 "미정"으로 두고 지어내지 않음
- **연구실 테마**: 실장.md에 `theme: lab` → 프론트 THEMES 프리셋(푸른 벽·타일·스틸 책상·주기율표 포스터·시약장 위 플라스크). 부서별 방 팔레트는 `theme:` 키로 프리셋 선택, `wall:`로 벽색만 덮어쓰기 가능
- **연구실 소품·가운** (2026-07-20 2차): lab이면 직원들이 **흰 가운**(카라만 본인 색 — 범례 색과 매칭), 직원 책상은 서류·전화 대신 **시험관 거치대(3색) + 삼각플라스크** 실험대, 오른쪽 벽에 시약병 선반. 웹 검색(전화 연출) 중에는 수화기 대신 **플라스크가 보글보글 기포** 애니메이션. 실장 책상 모니터는 관제용으로 유지
- **자리 비우기: 흡연부스·탕비실 커피** (2026-07-20, 전 부서 공통): 부서가 한가할 때(40초마다 60% 확률, busy·연기 중·탭 숨김·모션 축소 시 제외) 아무나 담배 또는 커피 중 랜덤으로 하나 골라 6~12초 쉬고 자리로 복귀 (`idleBreak`/`drawBreakProps`). 흡연부스 = 뒷줄 구석 유리 부스(간판·재떨이, 테마 색 따라감), 불씨 깜빡+연기 두 가닥. 커피 = 탕비실 앞에서 컵 들고 김 두 가닥, 가끔 홀짝 모션. 일이 들어오면 즉시 복귀. 전부 클라이언트 연출이라 서버·구독과 무관
- **사무실 PR (브랜치 감시)**: 팀원은 깃허브에 push만 하면 됨 (PR 버튼 불필요) — pr-watch.json에 `"watch": "branches"` + `"dept"` + `"path"(로컬 클론)` 등록 → 서버가 3분마다 브랜치 push 감지 → 코드 점검관이 git_fetch 후 git_diff(`base...target` 비교, 이번에 추가)로 main과 비교 점검 → 치명 없으면 "머지해도 됨" 보고. 병합은 별도 지시 시 통합담당이 git_fetch → main에서 origin/브랜치 병합 → push (결재). main/master push는 무시. claude-review-test로 감지→점검→반려 보고까지 검증 완료
- **PR 스코어보드** (2026-07-20): 실장.md에 `prboard: true`면 부서 탭 아래에 팀원별 push 접수 횟수 + 복합 기여도 % 표시 (`정연성 PR 4개 · 기여 52%`, 1등은 🏆+노란 테두리). **기여도 = (push 횟수 비율 + 변경 줄수 비율) / 2** — push 감지 때 compare API(직전 sha…새 sha, 새 브랜치는 main 기준)로 바뀐 줄 수를 받아 누적, 줄수 데이터가 없으면 횟수 비율만으로 계산. 저장 형식은 `{ prs, lines }`(구버전 숫자 파일도 호환). "저장소 용량 지분" 기준은 변경량과 중복 + 리팩토링당한 사람이 손해라서 제외. 팀원이 채팅·게시판에 이름을 남기면 그때부터 스코어보드에 오름 (PR 0개 시작, `office/pr-stats.json`에 영속). 집계는 push 감지 시 **깃허브 계정 → 커밋 작성자 이름 → 브랜치 이름** 순으로 등록된 이름과 대소문자 무시 대조 — 이름이 다르면 pr-watch.json 감시 항목에 `"authors": {"깃허브계정": "팀원 이름"}` 별명표로 이어줌 (별명표 매칭은 미등록자도 자동 등록). 안 맞으면 집계 보류 + 서버 로그. 갱신은 `pr_stats` SSE로 실시간

---

## 주요 기능

### 결재 게이트 (파괴적 작업 승인)

- `GATED_TOOLS`: `delete_note`, `archive_delete_scrap`, `git_merge`, `git_push`
- 실행 전 멈춤 → 메신저에 결재 카드(승인〔印〕/반려) → 승인 시에만 실행, 5분 무응답 자동 반려
- **승인은 사장만.** 이사·본부장 화면에는 버튼 없이 "사장님 결재 대기 중" 안내만 나오고, 서버도 `/confirm`을 403으로 막는다 (7단계 참고)
- 교훈: **"말로 한 승인 ≠ 시스템 게이트"** — 에이전트가 승인받았다고 믿어도 시스템이 막는다

### 세션제 (대회·프로젝트별 대화방)

- 클로드 세션처럼 부서마다 세션 선택 — 드롭다운 + ＋ 새 세션 + 🗑 삭제
- `office/sessions/<부서>/<id>.json`에 events(메신저 복원용) + histories(에이전트별 대화) 영속화 → **서버 재시작해도 대화 유지** (검증: 재시작 후 "물커톤 전용" 기억함)
- 마지막 세션 삭제 시 "기본 업무" 자동 생성
- 세션 삭제는 이사 이상 (2026-07-23 직급제 반영, 그전에는 사장 전용)
- **안읽음 뱃지** (2026-07-20): 보고 있지 않은 세션에 새 소식(user·report·assistant·결재 요청)이 오면 부서 탭에 개수 뱃지, 세션 드롭다운에 `● 제목 (n)` + 테두리 강조. 세션을 열면 읽음 처리. 라이브 이벤트만 세고 기록 복원은 안 셈 — 자동 리뷰가 "코드 리뷰" 세션에 쌓여도 놓치지 않게 하는 용도

### Claude Code 메모리 연동 (읽기 전용)

- `read_user_context` 도구로 `~/.claude/projects/.../memory/`를 열람 → 비서실이 사장님 배경(프로젝트·취향)을 인지
- 읽기만 가능, 수정 불가

### 대외활동 아카이버 연동

- `node:sqlite`(Node 내장)로 `~/activity-archiver/data/archive.db` 직접 접근
- 도구: list_scraps / list_activities / add_scrap / set_scrap_status / delete_scrap(결재)

### 직원별 모델 선택

- md의 `model:` 필드. 기본 `claude-opus-4-8`, 기록 담당은 `claude-haiku-4-5`
- 서버가 haiku/sonnet-4-5 계열엔 adaptive thinking·effort 파라미터를 자동 생략(400 방지)
- 설계 원칙: **역할마다 "지능 단가"를 다르게 책정**

### 안전장치

- 파일 접근은 홈 디렉터리 한정, `/.ssh`·`/.config/anthropic` 차단
- git은 `execFileSync` + 20초 타임아웃, `.git` 있는 경로만 허용
- 에이전트 루프 상한 20회

---

## 겪은 문제와 해결

| 문제 | 원인 | 해결 |
|---|---|---|
| "확인중"에서 몇 분간 멈춤 | ① 아카이버 등록 도구가 없어서 "작성"이 불가능 ② 웹검색 턴이 11분 걸려도 UI 무신호 | archive_add_scrap 추가, effort 도입(팀원 low·실장 medium), 30초 heartbeat(말풍선 경과 표시), 루프 상한. 왕복 14초로 단축 |
| 신입이 선배 책상 차지 | 파일명 정렬로 자리 배정 | md에 `order:` 필드(연차순) |
| 입석 직원 발생 | 책상 2개 고정 | 2열 6책상, 전원 착석 배치 |
| Haiku 400 에러 | adaptive thinking·effort 미지원 | 모델 보고 파라미터 자동 생략 |
| 03 REPL 종료 시 크래시 | stdin EOF 후 rl.question | try/catch로 정상 종료 |

---

## 실제 처리한 업무

- 기상청 기상·기후 AI 해커톤 1차 탈락 → 아카이버에서 스크랩 삭제 (결재 승인 후)
- MOVE-AI 물류 해커톤 본선 확정(8/13) — 개발실 "물커톤" 세션에서 관리
- 미래에셋 AI 페스티벌 정보 아카이버 등록

## 6단계 — 자동 점검 PM봇 + 팀 모드 + 구독제 전환 (2026-07-19)

### 자동 점검 (로컬 PM봇)

- **커밋 훅**: `office/install-hook.sh <저장소> [세션제목]` — post-commit이 개발실에 자동 접수, 코드 점검관이 점검 후 통과/반려 보고
- **PR 감시**: 서버가 3분마다 `gh pr list` 폴링 (`watch-pr.sh <계정/repo>`로 등록). 새 PR·새 커밋만 자동 접수, 충돌(CONFLICTING)은 치명 보고. 전부 내 맥에서만 실행
- 기획 담당 채용 (pm-skills의 PRD·프리모템·우선순위를 해커톤용으로 압축)

### 팀 공유 모드

- 같은 와이파이 팀원은 `http://<맥IP>:3010` 접속 — 접속 코드로 출근 (7단계에서 이름 자유입력 폐지)
- 결재 승인은 사장 전용 (당시엔 PIN(office/pin.txt) 방식 → 7단계에서 직급 판정으로 대체·PIN 폐기)
- **원격 공유**: `office/tunnel.sh` 실행 → trycloudflare.com 임시 공개 주소 발급 (Ctrl+C로 종료·주소 소멸). 상시 사용하려면 고정 주소(named tunnel)로 바꿔야 함 — 미착수
- **비서실 잠금**: 비서실은 사장에게만 보이고 접근 403 — 개인 메모리 보호. 판별 기준은 터널 경유 여부에서 **직급**으로 바뀜 (7단계)
- 삽질 기록: cloudflared 빠른 터널이 GET SSE를 통째로 버퍼링(이슈 #1449) → 프론트를 fetch POST 스트림으로 교체해 해결
- **부서 게시판**: 부서마다 팀원끼리 직접 쓰는 압정 메모지 게시판 (사무실 장면 아래). 전 접속자 실시간 동기화, 직원 호출이 없어 구독 소모 0. 저장은 office/boards/<부서>.json. 메모 떼기는 이사 이상, 본부장은 자기가 붙인 것만 — 프론트도 같은 기준으로 ✕를 숨긴다 (2026-07-23 직급제 반영, 그전에는 사장 전용)
- **답장 알림**: 실장 보고·업무 실패·결재 요청이 오면 조건 없이 항상 8비트 삐 소리 + 크롬 시스템 알림(권한 허용 시, localhost·https 터널만 — LAN IP는 비보안 컨텍스트라 소리만). 탭을 안 보고 있었으면 탭 제목에 📬도 표시. 권한 요청·AudioContext는 첫 클릭/입력(사용자 제스처) 때 준비
- **메신저 파일 첨부**: 입력창 옆 📎 → office/uploads/<부서>/에 저장(10MB 제한), 채팅에 다운로드 링크로 표시. 첨부 경로가 실장 지시문에 붙어 read_file 가진 직원이 내용을 읽음(텍스트 파일만). 원격 접속자도 동일하게 사용. HTML 등을 올려도 무조건 다운로드로 내려 XSS 차단, /say는 /upload가 발급한 stored 값만 신뢰(임의 경로 차단)

### 구독제 전환 (B안)

- 에이전트 실행을 `@anthropic-ai/sdk`(API 크레딧) → **Claude Agent SDK**(헤드리스 Claude Code)로 교체 — **Max 구독으로 과금, 크레딧 충전 불필요**
- 커스텀 도구는 인프로세스 MCP 서버로 래핑, 결재 게이트는 도구 핸들러 안에서 승인 대기 (MCP_TOOL_TIMEOUT 30분)
- Claude Code 내장 도구는 차단(BLOCKED_BUILTINS), web_search/web_fetch만 내장 WebSearch/WebFetch로 매핑
- 대화 맥락은 에이전트별 Claude Code 세션 id(resume)로 유지 — 기존 histories 방식 폐기
- 실전 검증: 메모 조회 22초, 커밋 훅→off-by-one 적발·반려 55초, 결재 게이트(오답 403 → 승인 후 실행)

## 7단계 — UI 전면 교체 + 직급제 (2026-07-22~23)

### 화면 재구축 — 도트 픽셀 폐기, Material 3 라이트

- 도트 픽셀(Galmuri·`#191b26`+노랑) 컨셉을 버리고 **Stitch 시안의 Material 3 라이트 토큰**을 그대로 옮김 (`surface #f7f9fb`, `primary #000`, `secondary #00687a`, Inter+Pretendard+Material Symbols). style.css `:root`가 시안의 토큰 표
- 방 이름 **연성 비서실 → TERRA 연구실** (로고 글자 `연` → `T`)
- **사무실 그림은 `office/public/scene.js`로 직접 그린 아이소메트릭 SVG.** AI 생성 3D 렌더(JPEG)는 안에 있는 사람을 움직일 수 없어서 폐기하고, 팔다리가 진짜 DOM 노드인 사람을 그림
  - 투영: `iso(x,y,z) = [OX + (x-y)*TILE*K, OY + (x+y)*TILE*0.5 - z*TILE]` (TILE 33, K 0.866). **x·y·z 전부 타일 단위** — z에 TILE 곱하는 걸 빠뜨려서 모든 가구가 납작해졌던 게 이번 최대 버그
  - 가구는 전부 `box()`(윗면+오른면+왼면) 하나로 그려 형태를 통일
  - 깊이 정렬은 `bands[Math.round(x+y)]` 밴딩 — 사람이 밴드 경계를 넘을 때만 DOM을 옮김
  - 걷기·타이핑은 CSS 키프레임. 관절은 `transform-box: fill-box; transform-origin: 50% 0`으로 어깨·엉덩이에서 회전
- 처음 입구에서 자리로 걸어 들어가는 입장 연출은 제거 (바로 착석)

### PM 봇 알림창 (물커톤실 전용)

- `prboard: true` 부서 하단에 **깃허브 활동 전용 다크 패널** — 배경은 Stitch 서버룸 렌더(`img/pm-bot-bg.jpg`) 위에 진한 막
- 기록 시점: `pollPRs()`의 브랜치 push·새 PR 감지 + **결재를 통과해 실제로 실행된** `git_merge`/`git_push`
- `office/git-log.json`에 부서당 최근 60건 영속, `git_log` SSE로 실시간. 종류별 색 (push 골드 / pr 시안 / merge 그린)

### 직급제 — 사장 > 이사 > 본부장 > AI 직원

친구들과 팀을 짜서 상시 사용하기로 하면서, "이름 자유 입력"을 **개인 접속 코드 인증**으로 교체.

- `office/members.json` = `[{ id, name, rank, code }]`, `office/tokens.json` = `{ 토큰: 사원id }`
- `POST /login`(이름+코드) → 토큰 발급 → 프론트가 `x-office-token` 헤더로 매 요청에 붙임 (app.js가 `fetch`를 한 번 감싸서 처리). **내 맥 localhost 접속은 코드 없이 사장**
- `whoIs(req)` / `rankOf(req)` / `isBoss(req)`가 유일한 권한 판정 지점. 기존 `isOwner`(터널 헤더 기준)는 여기서만 쓰임
- **이름은 서버가 명부에서 덮어쓴다** — 본문의 `name`은 못 믿는다 (위조 차단 검증 완료)
- **PIN 폐기** (2026-07-23): 결재를 직급으로 막게 되면서 `pin.txt`·`readPin()`·카드 안 PIN 입력칸을 전부 제거. 승인할 때 코드와 PIN 두 개를 외우는 게 번거로워서 한 겹으로 정리. 대신 접속 코드가 곧 결재 열쇠이므로 `office/members.json`·`tokens.json`을 `SENSITIVE_PATHS`에 넣어 직원의 `read_file`로도 못 읽게 막음

| | 사장 | 이사 | 본부장 |
|---|:---:|:---:|:---:|
| 지시·게시판·세션 생성 | ○ | ○ | ○ |
| 결재 승인 | ○ | ✕ | ✕ |
| 비서실 (개인 메모리) | ○ | ✕ | ✕ |
| 세션 삭제 | ○ | ○ | ✕ |
| 남의 게시판 메모 떼기 | ○ | ○ | 자기 것만 |

- 명부 관리는 `node office/members.mjs` (인자 없음=목록 / `add 이름 직급` / `code 이름` 재발급 / `rank 이름 직급` / `remove 이름`). 서버가 요청마다 파일을 다시 읽어서 **재시작 불필요**
- 직급은 AI에게도 전달 — 실장에게 가는 지시문이 `[이사 민수] …` 형태가 되고, "사장 > 이사 > 본부장 > AI 직원 순이며 이사님은 상급자다. 단 결재는 사장님 결재만 유효하다"는 문장이 붙음
- 검증: 오답 코드 403 / 미로그인 지시 401 / 이사·본부장 결재 403 / 비서실 403 / 메모·세션 삭제 직급별 403·200 / 이름 위조 차단

**곁다리로 잡은 버그**: `[hidden]` 속성이 `display: flex`에 밀려 안 먹히고 있었음(세션 🗑·사원증 카드). `[hidden] { display: none !important; }` 전역 규칙 한 줄로 해결.

## 다음 단계 후보 (미착수)

- **고정 주소** — 상시 사용하려면 trycloudflare 임시 주소(껐다 켜면 바뀜) 대신 Cloudflare named tunnel이 필요
- 크론 자동화 ("자동 출근")
- git_diff에 커밋 대비 diff 인자 추가 (커밋 직후엔 diff가 비어 점검관이 read_file로 우회 중)
