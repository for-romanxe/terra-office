node_modules/
.DS_Store

# 사원증 — 접속 코드 원본과 로그인 토큰. 절대 올리지 않는다.
office/members.json
office/tokens.json

# 어느 방에 쉘을 열어뒀는지 — 권한 상태라 레포에 박아두지 않는다.
# 클론한 쪽에서 모르는 채 켜진 상태로 시작하면 안 된다 (기본은 전부 꺼짐).
office/shell.json

# PR 감시 목록 — 어떤 저장소를 지켜보는지는 로컬 설정이다.
# 사적인 저장소명이 드러날 수 있어 레포에 두지 않는다.
office/pr-watch.json

# 사무실이 굴러가면서 쌓이는 데이터 (서버가 알아서 다시 만든다)
office/sessions/
office/uploads/
office/boards/
office/pr-seen.json
office/pr-stats.json
office/git-log.json

# 개인 메모함
notes/

# git 도구 시험용 껍데기
test-repo/
.test-remote.git/
